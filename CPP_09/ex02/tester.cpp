
#include "PmergeMe.hpp"

int main()
{


    // int i = 0;
    // int buf;
    // std::vector<int> nums;
    // while(std::cin >> buf)
    //     nums.push_back(buf);
    // while (i < nums.size() - 1 &&  nums[i] <= nums[i + 1])
    //     i++;
    // if (i == nums.size() - 1)
    //     std::cout << "CORRECT\n";
    // else
    //     std::cout << "WRONG\n";
}